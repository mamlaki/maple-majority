import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Maple Majority ",
  description: "Experience and manage a Canadian federal election campaign of your choice! Choose your party, answer questions, make critical decisions, debate, and travel across the country to fight for the most seats in Parliament. Maybe you'll even get a majority.",
  keywords: ["Canada", "politics", "election", "campaign", "game", "simulation", "Parliment", "debate", "decisions", "strategy"]
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
